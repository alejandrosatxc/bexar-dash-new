import styles from '../styles/Home.module.css'

export default function Home({master}) {
  return (
        <div className={styles.grid}>
          <h2>Tap on a button above to see the relevant data</h2>
        </div>
  )
}
